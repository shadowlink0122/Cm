// 2. 変数と基本的な演算
int main() {
    // 整数型
    int x = 10;
    int y = 20;
    int sum = x + y;

    // 浮動小数点
    float pi = 3.14;
    float radius = 5.0;
    float area = pi * radius * radius;

    // ブール型
    bool is_valid = true;
    bool result = (x < y) && is_valid;

    // 文字型
    char grade = 'A';

    return 0;
}