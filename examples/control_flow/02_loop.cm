// ループの脱糖確認用
int main() {
    int sum = 0;

    // for文（HIRでwhile文に変換される）
    for (int i = 0; i < 10; i = i + 1) {
        sum = sum + i;
    }

    // while文
    int j = 0;
    while (j < 5) {
        sum = sum + j;
        j = j + 1;
    }

    return sum;
}