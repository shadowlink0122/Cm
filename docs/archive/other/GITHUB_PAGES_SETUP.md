[English](GITHUB_PAGES_SETUP.en.html)

# GitHub Pages セットアップガイド

このガイドでは、Cm言語のドキュメントをGitHub Pagesで公開する方法を説明します。

---

## 📋 前提条件

- GitHubアカウント
- Cmリポジトリへのアクセス権
- GitHubリポジトリの設定変更権限

---

## 🚀 セットアップ手順

### 1. GitHub Pagesを有効化

1. GitHubリポジトリページにアクセス
2. **Settings** タブをクリック
3. 左サイドバーの **Pages** をクリック
4. **Source** セクションで以下を設定:
   - **Branch:** `main` (または `master`)
   - **Folder:** `/docs`
5. **Save** をクリック

### 2. 設定ファイルの確認

以下のファイルが正しく配置されていることを確認:

```
docs/
├── _config.yml          # Jekyll設定ファイル
├── index.md             # トップページ
├── tutorials/           # チュートリアル
│   ├── README.md
│   ├── basics/
│   ├── types/
│   ├── advanced/
│   └── compiler/
└── releases/            # リリースノート
    ├── README.md
    └── v0.10.0.md
```

### 3. _config.yml の編集

`docs/_config.yml` を開き、以下を自分の環境に合わせて変更:

```yaml
# Site settings
title: Cm Programming Language
url: "https://yourusername.github.io"  # ← あなたのGitHubユーザー名に変更
baseurl: "/Cm"                          # ← リポジトリ名に変更
```

### 4. コミット＆プッシュ

```bash
cd /path/to/Cm
git add docs/
git commit -m "Setup GitHub Pages"
git push origin main
```

### 5. デプロイの確認

1. GitHubリポジトリの **Actions** タブを確認
2. "pages-build-deployment" ワークフローが成功するのを待つ（通常1-2分）
3. **Settings > Pages** に表示されるURLにアクセス

**公開URL例:**
```
https://yourusername.github.io/Cm/
```

---

## 📖 公開されるページ

以下のページがGitHub Pagesで閲覧可能になります:

### トップページ
- **URL:** `https://yourusername.github.io/Cm/`
- **ファイル:** `docs/index.md`
- プロジェクト概要、クイックスタート、リンク集

### チュートリアル
- **URL:** `https://yourusername.github.io/Cm/tutorials/`
- **ファイル:** `docs/tutorials/README.md` + 各チュートリアル
- 基本から応用までの学習コンテンツ

#### サブセクション
- `/tutorials/basics/` - 基本編
- `/tutorials/types/` - 型システム編
- `/tutorials/advanced/` - 応用編
- `/tutorials/compiler/` - コンパイラ編

### リリースノート
- **URL:** `https://yourusername.github.io/Cm/releases/`
- **ファイル:** `docs/releases/README.md` + 各バージョン
- バージョン履歴と変更点

---

## 🎨 カスタマイズ

### テーマの変更

`docs/_config.yml` で別のテーマを選択可能:

```yaml
# GitHub Pages サポートテーマ
theme: jekyll-theme-cayman        # 現在のテーマ
# theme: jekyll-theme-minimal
# theme: jekyll-theme-slate
# theme: jekyll-theme-architect
```

利用可能なテーマ一覧: https://pages.github.com/themes/

### ナビゲーションの変更

各マークdownファイルのフロントマターで調整:

```yaml
---
layout: default
title: ページタイトル
parent: 親ページ名        # 階層構造を作る場合
nav_order: 1              # 表示順序
has_children: true        # 子ページがある場合
---
```

### カスタムCSS/JS

`docs/assets/` ディレクトリを作成してカスタムスタイルを追加可能:

```
docs/
├── assets/
│   ├── css/
│   │   └── custom.css
│   └── js/
│       └── custom.js
```

`_config.yml` に追加:

```yaml
# Custom CSS
sass:
  sass_dir: assets/css
  style: compressed
```

---

## 🔧 トラブルシューティング

### ページが表示されない

**原因1: Pagesが有効化されていない**
- Settings > Pages で設定を確認

**原因2: ビルドエラー**
- Actions タブでエラーログを確認
- `_config.yml` の構文エラーをチェック

**原因3: URLが間違っている**
- `baseurl` の設定を確認（先頭に `/` が必要）

### スタイルが適用されない

**原因: 相対パスの問題**

```yaml
# _config.yml
baseurl: "/Cm"  # 必ず設定
```

リンクは以下のように記述:

```markdown
[チュートリアル]({{ site.baseurl }}/tutorials/)
```

### フロントマターのエラー

フロントマターは必ず以下の形式:

```yaml
---
title: タイトル
---
```

- 開始/終了は `---` (ハイフン3つ)
- YAMLの文法に従う
- インデントはスペース（タブ不可）

---

## 🔄 更新手順

### ドキュメントの更新

1. `docs/` 内のファイルを編集
2. コミット＆プッシュ

```bash
git add docs/
git commit -m "Update documentation"
git push origin main
```

3. GitHub Actionsが自動的にビルド・デプロイ（1-2分）

### 新しいページの追加

1. 適切なディレクトリに `.md` ファイルを作成
2. フロントマターを追加

```markdown
---
layout: default
title: 新しいページ
parent: 親ページ
nav_order: 10
---

# 新しいページ

内容...
```

3. コミット＆プッシュ

---

## 📊 アクセス解析

### GitHub Insights

リポジトリの **Insights > Traffic** でアクセス状況を確認可能:

- ページビュー数
- ユニークビジター数
- 参照元サイト

### Google Analytics（オプション）

`_config.yml` にトラッキングIDを追加:

```yaml
# Google Analytics
google_analytics: UA-XXXXXXXXX-X
```

---

## ✅ チェックリスト

初回セットアップ時:

- [ ] GitHub Pages を有効化（Settings > Pages）
- [ ] `_config.yml` の `url` と `baseurl` を編集
- [ ] 変更をコミット＆プッシュ
- [ ] デプロイ完了を確認（Actions タブ）
- [ ] 公開URLで動作確認
- [ ] 全ページのリンクをテスト
- [ ] モバイル表示を確認

---

## 📚 参考リンク

- [GitHub Pages公式ドキュメント](https://docs.github.com/pages)
- [Jekyll公式ドキュメント](https://jekyllrb.com/docs/)
- [Jekyll テーマ一覧](https://pages.github.com/themes/)
- [Markdown記法ガイド](https://guides.github.com/features/mastering-markdown/)

---

## 💬 サポート

問題が発生した場合:

1. [GitHub Discussions](https://github.com/yourusername/Cm/discussions) で質問
2. [Issues](https://github.com/yourusername/Cm/issues) でバグ報告
3. このガイドの「トラブルシューティング」セクションを確認

---

**最終更新:** 2025-12-16  
**対象バージョン:** GitHub Pages + Jekyll (Cayman theme)