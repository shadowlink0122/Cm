# 基本編 - 演算子

**難易度:** 🟢 初級  
**所要時間:** 15分

## 📚 この章で学ぶこと

- 算術演算子
- 比較演算子
- 論理演算子
- 複合代入演算子
- インクリメント/デクリメント

---

## 算術演算子

### 基本演算

```cm
int a = 10, b = 3;

int sum = a + b;      // 13 (加算)
int diff = a - b;     // 7  (減算)
int prod = a * b;     // 30 (乗算)
int quot = a / b;     // 3  (除算)
int rem = a % b;      // 1  (剰余)
```

### 浮動小数点演算

```cm
double x = 10.0, y = 3.0;

double sum = x + y;   // 13.0
double quot = x / y;  // 3.333...
// double rem = x % y;  // エラー: %は整数のみ
```

### 単項演算子

```cm
int x = 10;
int neg = -x;         // -10 (符号反転)
int pos = +x;         // 10  (符号維持)
```

---

## 比較演算子

```cm
int a = 10, b = 20;

bool eq = (a == b);   // false (等しい)
bool ne = (a != b);   // true  (等しくない)
bool lt = (a < b);    // true  (より小さい)
bool le = (a <= b);   // true  (以下)
bool gt = (a > b);    // false (より大きい)
bool ge = (a >= b);   // false (以上)
```

### 文字列比較

```cm
string s1 = "abc";
string s2 = "abc";
string s3 = "def";

bool same = (s1 == s2);     // true
bool diff = (s1 != s3);     // true
```

---

## 論理演算子

### AND (&&)

```cm
bool a = true, b = false;

bool result = a && b;  // false
// 両方trueの時だけtrue

if (x > 0 && x < 10) {
    println("xは1から9の間");
}
```

### OR (||)

```cm
bool result = a || b;  // true
// どちらかtrueならtrue

if (x < 0 || x > 100) {
    println("範囲外");
}
```

### NOT (!)

```cm
bool flag = true;
bool negated = !flag;  // false

if (!is_empty) {
    println("空ではない");
}
```

### 短絡評価

```cm
// &&は左がfalseなら右を評価しない
bool result = false && expensive_function();  // 呼ばれない

// ||は左がtrueなら右を評価しない
bool result = true || expensive_function();   // 呼ばれない
```

---

## 複合代入演算子

```cm
int x = 10;

x += 5;   // x = x + 5;  → 15
x -= 3;   // x = x - 3;  → 12
x *= 2;   // x = x * 2;  → 24
x /= 4;   // x = x / 4;  → 6
x %= 4;   // x = x % 4;  → 2
```

---

## インクリメント/デクリメント

### 後置演算子

```cm
int i = 5;
int a = i++;  // a = 5, i = 6 (使用後に加算)
int b = i--;  // b = 6, i = 5 (使用後に減算)
```

### 前置演算子

```cm
int i = 5;
int a = ++i;  // a = 6, i = 6 (加算後に使用)
int b = --i;  // b = 5, i = 5 (減算後に使用)
```

### ループでの使用

```cm
// 後置（一般的）
for (int i = 0; i < 10; i++) {
    println("{}", i);
}

// 前置（同じ結果）
for (int i = 0; i < 10; ++i) {
    println("{}", i);
}
```

---

## 演算子の優先順位

優先度（高→低）:

1. `()` - 括弧
2. `++`, `--`, `!`, `+`(単項), `-`(単項) - 単項演算子
3. `*`, `/`, `%` - 乗除
4. `+`, `-` - 加減
5. `<`, `<=`, `>`, `>=` - 比較
6. `==`, `!=` - 等価
7. `&&` - 論理AND
8. `||` - 論理OR
9. `=`, `+=`, `-=`, etc. - 代入

### 例

```cm
int result = 2 + 3 * 4;     // 14 (3*4が先)
int result = (2 + 3) * 4;   // 20 (括弧が優先)

bool b = 5 > 3 && 10 < 20;  // true
bool b = 5 > 3 || false && true;  // true (&&が先)
```

---

## 三項演算子

条件式 `?` 真の値 `:` 偽の値

```cm
int max = (a > b) ? a : b;

string status = (age >= 20) ? "Adult" : "Minor";

// ネスト可能（非推奨）
int sign = (x > 0) ? 1 : (x < 0) ? -1 : 0;
```

---

## よくある間違い

### ❌ 代入と比較の混同

```cm
int x = 10;
if (x = 5) {  // 警告: 代入している
    // xは5になる
}

// 正しい
if (x == 5) {  // 比較
    // ...
}
```

### ❌ 整数除算の精度

```cm
int result = 5 / 2;  // 2 (整数除算)

// 正しい
double result = 5.0 / 2.0;  // 2.5
```

### ❌ 論理演算子とビット演算子

```cm
bool result = true & false;  // エラー: &はビット演算
bool result = true && false; // OK: 論理AND
```

---

## 練習問題

### 問題1: 計算機
2つの数値の四則演算を行う関数を書いてください。

<details>
<summary>解答例</summary>

```cm
int main() {
    int a = 15, b = 4;
    
    println("{} + {} = {}", a, b, a + b);
    println("{} - {} = {}", a, b, a - b);
    println("{} * {} = {}", a, b, a * b);
    println("{} / {} = {}", a, b, a / b);
    println("{} % {} = {}", a, b, a % b);
    
    return 0;
}
```
</details>

### 問題2: 閏年判定
年が閏年かどうかを判定してください。

<details>
<summary>解答例</summary>

```cm
bool is_leap_year(int year) {
    return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
}

int main() {
    int year = 2024;
    if (is_leap_year(year)) {
        println("{} is a leap year", year);
    } else {
        println("{} is not a leap year", year);
    }
    return 0;
}
```
</details>

---

## 次のステップ

✅ 演算子の種類を理解した  
✅ 優先順位がわかった  
⏭️ 次は [制御構文](control-flow.md) を学びましょう

## 関連リンク

- [演算子オーバーロード](../advanced/operators.md)
- [型変換](../types/conversions.md)

---

**前の章:** [変数と型](variables.md)  
**次の章:** [制御構文](control-flow.md)
