# ドキュメント再編成サマリー

## 実行日: 2026-01-12

## 実行内容

v0.11.0の実装完了に伴い、ドキュメントの整理と再配置を実施しました。

## 再配置の詳細

### 1. v0.11.0実装済みドキュメント → docs/archive/v0.11.0/
**実装完了した機能:**
- **JITコンパイラ** (001-005)
  - jit_engine.cpp/hpp として実装済み
  - 基本的なJIT実行環境が構築完了

- **const generics** (028)
  - `const N: int` 構文のサポート完了
  - パーサーから型システムまで統合済み

- **マクロシステム** (060, 063-064)
  - macro/expander.cpp として実装
  - 基本的なマクロ展開機能が動作中

- **配列最適化** (006-010, 一部実装)
  - 配列フラット化の基本構造実装
  - 多次元配列サポート (ast/types.hpp)

- **その他v0.11.0関連ドキュメント**
  - ベンチマーク分析
  - 最適化技術
  - リリースノート

**移動ファイル数:** 49ファイル

### 2. 未実装ドキュメント → docs/design/v0.12.0/

#### claude/ サブディレクトリ (44ファイル)
**主な未実装機能:**
- **インライン展開** (040-046, 050-051)
  - インライン関数の展開機能
  - 最適化レベルでの制御

- **ランタイムからstdlibへの移行** (030-033)
  - ランタイム関数の標準ライブラリ化

- **BNFパーサーリファクタリング** (071-074)
  - パーサーの文法定義改善

- **インターフェース継承** (076-077)
  - インターフェースの継承と合成

#### gemini/ サブディレクトリ (17ファイル + 2サブディレクトリ)
**主な提案内容:**
- コンパイラアーキテクチャ改善
- 非同期処理設計 (async/await)
- enum拡張
- コアライブラリ設計
- JSコンパイラ関連

## ディレクトリ構造

```
docs/
├── archive/
│   └── v0.11.0/          # 実装済み機能のドキュメント (49ファイル)
│
└── design/
    └── v0.12.0/
        ├── claude/        # Claude提案の未実装機能 (44ファイル)
        ├── gemini/        # Gemini提案の未実装機能 (17ファイル)
        │   ├── async/     # 非同期処理設計
        │   └── js_compiler/ # JSコンパイラ設計
        └── macros.md      # 既存のマクロ設計ドキュメント
```

## 効果

1. **明確な実装状況管理**: v0.11.0で実装された機能と未実装機能が明確に分離
2. **プリフィクス競合の解消**: claude/とgemini/フォルダ分離により番号重複を回避
3. **次期バージョン計画の明確化**: v0.12.0で実装すべき機能が整理された

## 次のステップ

v0.12.0では以下の優先度で実装を検討:
1. インライン展開機能（パフォーマンス向上に直結）
2. ランタイムのstdlib移行（メンテナンス性向上）
3. インターフェース拡張（言語表現力の向上）
4. 非同期処理サポート（モダンな並行処理）