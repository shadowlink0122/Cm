# パッケージマネージャ・エコシステム設計

## 概要

Cm言語は、統合パッケージマネージャ・バージョン管理ツール **gen** (弦) を提供します。

> 💡 **名前の由来**: Cb/Cmが音楽（コード名）に由来することから、「弦」をモチーフにしています。Ruby/gemへのオマージュでもあります。

## エコシステム構成

```
┌─────────────────────────────────────────────────────────────┐
│                    Cm Ecosystem                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   cm CLI    │  │   gen CLI   │  │   cm-doc    │         │
│  │ (Compiler)  │  │ (Package)   │  │ (Doc Gen)   │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│         │                │                │                 │
│         └────────────────┼────────────────┘                 │
│                          │                                  │
│                          ▼                                  │
│                  ┌───────────────┐                          │
│                  │   Registry    │                          │
│                  │ packages.cm   │                          │
│                  └───────────────┘                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## バージョン管理

### Cmバージョン管理

```bash
# Cmのバージョン一覧
gen list versions

# 特定バージョンのインストール
gen install cm@1.0.0

# グローバルデフォルト設定
gen use 1.0.0

# プロジェクト固有バージョン設定
gen use 0.9.0 --local  # .cm-version ファイル生成

# 現在のバージョン確認
gen version
```

### .cm-version ファイル

```
1.0.0
```

プロジェクトルートに配置すると、そのプロジェクトでは指定バージョンのCmを使用。

---

## インストールモード

### 1. グローバルインストール

システム全体で使用するツール・ライブラリ用。

```bash
# グローバルにパッケージをインストール
gen install -g cm-fmt        # フォーマッター
gen install -g cm-lsp        # Language Server
gen install -g cm-test       # テストランナー

# インストール先
# Unix:    ~/.cm/bin/
# Windows: %USERPROFILE%\.cm\bin\

# 一覧表示
gen list -g

# アンインストール
gen uninstall -g cm-fmt
```

### 2. ローカルインストール（プロジェクト単位）

プロジェクト固有の依存関係用。**推奨**。

```bash
# プロジェクト初期化
gen init my-project

# 依存関係追加
gen add cm-json
gen add cm-http --dev    # 開発時のみ

# インストール先
# ./cm_modules/

# ロックファイル生成
# Cm.lock (自動生成、コミット対象)
```

---

## プロジェクト構成

### Cm.toml

```toml
[package]
name = "my-app"
version = "0.1.0"
authors = ["Your Name <you@example.com>"]
edition = "2025"
description = "My Cm application"
license = "MIT"
repository = "https://github.com/example/my-app"

# Cm言語バージョン要件
cm = ">=1.0.0"

[dependencies]
cm-std = "1.0"           # 標準ライブラリ
cm-json = "0.5"          # JSONパーサー
cm-http = { version = "0.3", optional = true }

[dev-dependencies]
cm-test = "1.0"          # テストフレームワーク
cm-bench = "0.2"         # ベンチマーク

[build-dependencies]
cm-codegen = "0.2"       # コード生成

[features]
default = ["std"]
std = ["cm-std"]
web = ["cm-http"]

# ターゲット設定
[target.native]
backend = "rust"

[target.web]
backend = "wasm"
```

### ディレクトリ構造

```
my-project/
├── Cm.toml              # プロジェクト設定
├── Cm.lock              # ロックファイル（自動生成）
├── .cm-version          # Cmバージョン指定（オプション）
├── src/
│   ├── main.cm          # エントリーポイント（バイナリ）
│   └── lib.cm           # エントリーポイント（ライブラリ）
├── cm_modules/          # ローカル依存（.gitignore）
├── tests/
│   └── integration_test.cm
├── benches/
│   └── benchmark.cm
└── examples/
    └── example.cm
```

---

## コマンドライン

### gen コマンド

```bash
# プロジェクト管理
gen init [name]          # 新規プロジェクト
gen init --lib [name]    # ライブラリプロジェクト

# パッケージ管理
gen add <package>        # 依存追加
gen add <package> --dev  # 開発依存追加
gen remove <package>     # 依存削除
gen update               # 依存更新
gen install              # Cm.lockから復元

# グローバル
gen install -g <package> # グローバルインストール
gen uninstall -g <package>
gen list -g              # グローバル一覧

# バージョン管理
gen list versions        # 利用可能なCmバージョン
gen install cm@1.0.0     # Cmバージョンインストール
gen use 1.0.0            # グローバルバージョン設定
gen use 0.9.0 --local    # ローカルバージョン設定

# 情報
gen list                 # ローカル依存一覧
gen info <package>       # パッケージ詳細
gen search <query>       # パッケージ検索

# パブリッシュ
gen login                # レジストリログイン
gen publish              # パッケージ公開
```

### cm コマンド

```bash
# ビルド・実行
cm build                 # デバッグビルド
cm build --release       # リリースビルド
cm build --target web    # Webターゲット

cm run                   # 実行
cm run --release         # リリース実行

# テスト
cm test                  # 全テスト
cm test unit             # 特定テスト

# その他
cm check                 # 型チェックのみ
cm doc                   # ドキュメント生成
cm fmt                   # フォーマット
cm lint                  # リント
```

---

## モジュールシステム

### インポート (C++風)

```cpp
// main.cm
import utils;
import net::http;
import cm_json as json;  // 外部パッケージ

int main() {
    auto data = json::parse("{}");
    auto client = http::Client();
    return 0;
}
```

### エクスポート

```cpp
// lib.cm
export int publicFunction() { return 42; }
int privateFunction() { return 0; }  // export なしは非公開

export struct PublicStruct {
    int value;
};
```

---

## レジストリ

### 公式レジストリ

```
https://gen.cm-lang.org
```

### プライベートレジストリ

```toml
# ~/.cm/config.toml
[registries]
private = { url = "https://gen.mycompany.com" }

# Cm.toml
[dependencies]
internal-lib = { version = "1.0", registry = "private" }
```

---

## キャッシュ構造

```
~/.cm/
├── bin/                 # グローバルインストール済みバイナリ
├── toolchains/          # Cmバージョン別ツールチェーン
│   ├── 1.0.0/
│   └── 0.9.0/
├── registry/            # パッケージインデックスキャッシュ
├── cache/               # ダウンロード済みパッケージ
└── config.toml          # グローバル設定

project/
├── cm_modules/          # ローカル依存（プロジェクト固有）
└── target/              # ビルド成果物
    ├── debug/
    ├── release/
    └── web/
```

---

## セキュリティ

- **チェックサム**: SHA-256によるパッケージ整合性確認
- **署名**: パッケージ署名検証（将来）
- **監査**: `gen audit` で脆弱性チェック
- **ロックファイル**: 再現可能なビルド保証
