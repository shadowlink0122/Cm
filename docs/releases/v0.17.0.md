---
title: v0.17.0
parent: Release Notes
nav_order: -3
---

# Cm v0.17.0 リリースノート（開発中）

**前バージョン:** v0.16.2

## 🎯 テーマ: JS/TSエコシステム連携（npmパッケージ・React/Nodeへの道）

## ✨ 改善

### TypeScriptバックエンド（--target=ts）: 型注釈付き出力

- **`cm compile --target=ts` でTypeScriptを出力できるようになった**: Cmは静的型付け言語のため、型情報を保ったTS出力を第一級サポートする。`--target=js` と同一のコード生成に加え、関数シグネチャ・ローカル変数・構造体フィールドへ型注釈を付与し、`struct` を `export interface` として出力する
- **型写像**: Cmの数値型は全て `number`、`string`→`string`、`bool`→`boolean`、`struct`/`enum`→名前（interface宣言）、関数型→`(a: T, ...) => R`、配列→`T[]`、ポインタ/参照→`any`（JS/TSに対応概念がなくランタイム表現が混在するため安全側）、async関数の戻り値→`Promise<T>`
- **型検査を通る出力**: `@types/node` へ依存せず型検査できるよう `require`/`process`/`module`/`console` をambient宣言する。生成TSは `tsc --noEmit --lib es2018` で型エラーなく通る
- **実行**: `cm run --target=ts` は型注釈を除去したJSツイン（生成JSと同一）を生成してNodeで実行する（Node v23.6未満でも動作）。TSとJSはランタイム挙動が同一
- **バグ発見と修正**: TSの型検査ゲートが、`with Css` 自動生成メソッド `to_css` の戻り値をvoidと誤判定していた既存バグ（JS出力にも潜在）を検出。戻り値ローカルの宣言型を型の権威として優先するよう修正した
- **テスト**: `tests/ts/`（`make test-ts`）を新設。焦点テスト（型注釈生成・tsc型検査・実行出力）に加え、jsテストコーパス全体を `--target=ts` でコンパイル・型検査する広域ゲートで全プログラムが有効なTSを生成することを保証する。CIのToolingジョブへ組み込み（npx経由でtsc取得）
- チュートリアル `docs/tutorials/{ja,en}/compiler/js/index.md` にTypeScript出力の節を追加（推奨ターゲットとして案内）

### JSバックエンド: npmパッケージ連携の確立と関数型フィールドの呼び出し対応

- **`use "パッケージ名" { 宣言 }` によるnpm/Node組み込みモジュール連携をテスト・ドキュメントで正式化**: 構造体はJSオブジェクトとしてフィールド名保持で相互に渡り（変換不要）、関数ポインタ・ラムダはJS関数としてそのまま渡せることを検証した
- **構造体の関数型フィールドをメソッド構文で呼び出せるようになった（`obj.field(args)`）**: JSオブジェクトのメソッド（関数値プロパティ）をCmから自然に起動できる。呼び出し先をメンバPlaceのまま保持して `obj.method(args)` を直接出力するため、**JSのthis束縛が保持される**。native/wasm/jitでも同様に動作する（HirCall::indirect_callee新設、型チェッカのフィールドフォールバック、文字列補間ミニパイプライン対応を含む）
- **LLVMコード生成のバグ修正**: 関数参照代入のSSAショートカットが投影を無視し、`ops.apply = f` のようなフィールド代入で構造体ローカルのスロットが関数値に上書きされてSEGVになっていた問題を修正（native/wasm/jitの関数型フィールドが動作するようになった）
- **JS FFIテスト基盤**: ローカルfixtureパッケージ（`tests/js/ffi/node_modules/cmtestpkg`、npm install不要）を新設し、テストランナーが `NODE_PATH` で解決する。構造体互換・コールバック・メソッドthis束縛・組み込みモジュールの4テストと全バックエンド共通の関数型フィールドテストを追加
- チュートリアル `docs/tutorials/{ja,en}/compiler/js/npm-interop.md` を新設。実装設計は `docs/archive/v0.17.0/01_js_npm_interop.md`、React/TS対応ロードマップは `docs/design/js_interop_roadmap.md`
