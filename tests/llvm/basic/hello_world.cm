// 最小限のHello Worldプログラム
// ターゲット: Native

int main() {
    // 基本的な算術演算
    int x = 10;
    int y = 20;
    int z = x + y;

    // 条件分岐
    if (z == 30) {
        return 0;  // 成功
    } else {
        return 1;  // エラー
    }
}