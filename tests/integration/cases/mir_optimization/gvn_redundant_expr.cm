// GVN: 同一式 p+q の再計算が2箇所→1箇所になり、2回目はコピーへ置き換わる。
// 注意: コピー伝播は関数引数を保守的に除外するため、引数を直接使う式
// （a+b）は正規化されずGVNの対象にならない。単一代入ローカル経由が対象
int f(int a, int b) {
    int p = a;
    int q = b;
    int x = p + q;
    int y = p + q;
    return x * y;
}
