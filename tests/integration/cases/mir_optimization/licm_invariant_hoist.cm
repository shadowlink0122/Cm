// LICM: ループ不変式 x*y がプリヘッダへ巻き上げられる（文は移動し削除されない）
int f(int x, int y, int n) {
    int acc = 0;
    int i = 0;
    while (i < n) {
        int inv = x * y;
        acc = acc + inv;
        i = i + 1;
    }
    return acc;
}
