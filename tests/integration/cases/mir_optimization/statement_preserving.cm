// 文保存モード（fold_terminators=false）の検証ケース:
// 定数条件分岐・定数式・恒等式を含むが、文数とCFG形状は変わってはならない
int f(int a) {
    int c = 2 * 3 + 4;
    int y = a * 1;
    if (c > 5) {
        y = y + c;
    } else {
        y = y - c;
    }
    return y;
}
