import std::io;

int main() {
    int[300][300] a;
    int[300][300] b;
    int[300][300] bt;  // B転置
    int[300][300] c;
    
    // 初期化
    for (int i = 0; i < 300; i++) {
        for (int j = 0; j < 300; j++) {
            a[i][j] = 1;
            b[i][j] = 1;
            c[i][j] = 0;
        }
    }
    
    // B行列を転置: bt[j][i] = b[i][j]
    for (int i = 0; i < 300; i++) {
        for (int j = 0; j < 300; j++) {
            bt[j][i] = b[i][j];
        }
    }
    
    // 最適化された行列乗算（転置済みB使用）
    // a[i][k] * bt[j][k] → 両方kに沿って連続アクセス！
    for (int i = 0; i < 300; i++) {
        for (int j = 0; j < 300; j++) {
            int sum = 0;
            for (int k = 0; k < 300; k++) {
                sum = sum + a[i][k] * bt[j][k];
            }
            c[i][j] = sum;
        }
    }
    
    println("done {c[0][0]}");
    return 0;
}
