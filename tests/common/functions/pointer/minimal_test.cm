import std::io::println;
// 最小限の関数ポインタテスト

int add(int a, int b) {
    return a + b;
}

int main() {
    // 関数ポインタの宣言と代入
    const int*(int, int) op = &add;

    // 関数ポインタ経由での呼び出し
    const int result = op(3, 2);
    println("{result}");
    return 0;
}
