# Linux x86_64 LLVM O3 SIGILL issue
# Root cause: LLVM O3 unreachable code optimization generates ud2 instruction
# This is a known platform-specific issue that doesn't affect macOS or ARM64
# Workaround: mir_to_llvm.cpp includes reachability analysis to skip unreachable blocks
llvm-o3:linux:x86_64
