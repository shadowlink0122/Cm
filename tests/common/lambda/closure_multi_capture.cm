import std::io::println;

// 複数変数をキャプチャするクロージャのテスト
int main() {
    int a = 10;
    int b = 20;

    // 2つの外部変数をキャプチャ
    int*(int) adder = (int x) => { return x + a + b;
    };

    int result = adder(5);
    // 5 + 10 + 20 = 35
    println("result: {result}");

    return 0;
}
