import std::io::println;

// クロージャ（変数キャプチャ）のテスト
int main() {
    const int multiplier = 3;

    // 外部変数をキャプチャするラムダ
    const int*(int) multiply = (int x) => { return x * multiplier;
    };

    const int result = multiply(5);
    // 5 * 3 = 15
    println("result: {result}");

    return 0;
}
