// 多次元配列リテラルの要素検査: 正しい要素と拡大変換は許可される（内側要素まで再帰検査する）
import std::io::println;

int main() {
    // 正しい int の入れ子
    int[2][2] m = [[1, 2], [3, 4]];
    println(m[1][1]);

    // 内側要素の拡大変換（tiny -> int）は許可
    tiny t = 5;
    int[2][2] w = [[t, 2], [3, 4]];
    println(w[0][0]);

    return 0;
}
