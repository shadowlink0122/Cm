import std::io::println;
// 文字列メソッドのテスト

int main() {
    const string s = "Hello, World!";

    // len() - 文字列の長さ
    const uint len = s.len();
    println("len: {len}");

    // charAt() - 指定位置の文字
    const char c = s.charAt(0);
    println("charAt(0): {c}");

    // substring() - 部分文字列
    const string sub = s.substring(0, 5);
    println("substring(0,5): {sub}");

    // indexOf() - 検索
    const int idx = s.indexOf("World");
    println("indexOf(World): {idx}");

    // toUpperCase() - 大文字変換
    const string upper = s.toUpperCase();
    println("toUpperCase: {upper}");

    // toLowerCase() - 小文字変換
    const string lower = s.toLowerCase();
    println("toLowerCase: {lower}");

    // trim() - 空白除去
    const string padded = "  trimmed  ";
    const string trimmed = padded.trim();
    println("trim: [{trimmed}]");

    // startsWith() - 先頭一致
    const bool sw = s.startsWith("Hello");
    println("startsWith(Hello): {sw}");

    // endsWith() - 末尾一致
    const bool ew = s.endsWith("!");
    println("endsWith(!): {ew}");

    // includes() - 含む判定
    const bool inc = s.includes("World");
    println("includes(World): {inc}");

    // repeat() - 繰り返し
    const string rep = "ab".repeat(3);
    println("repeat(3): {rep}");

    // replace() - 置換
    const string repl = s.replace("World", "Cm");
    println("replace: {repl}");

    return 0;
}
