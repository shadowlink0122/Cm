// Basic defer test
import std::io::println;

int main() {
    const int x = 1;
    println(x);

    defer println(100);
    defer println(200);
    defer println(300);

    const int y = 2;
    println(y);

    const int z = 3;
    println(z);
    return 0;
}
