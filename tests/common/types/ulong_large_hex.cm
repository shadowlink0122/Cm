// Bug#6修正テスト: 大きな16進リテラルがstoll範囲外でクラッシュしないことを確認
// std::stoll → std::stoull変更の回帰テスト
import std::io::println;

int main() {
    // i64の最大値ちょうど（これはstollでもOK）
    const ulong max_i64 = 0x7FFFFFFFFFFFFFFF;
    println(max_i64);

    // i64の最大値+1（stollではout_of_range）
    const ulong over_i64 = 0x8000000000000000;
    println(over_i64);

    // u64の最大値
    const ulong max_u64 = 0xFFFFFFFFFFFFFFFF;
    println(max_u64);

    // 大きなhex値（stollでは範囲外）
    const ulong big1 = 0xABCDEF0123456789;
    println(big1);

    // ビットパターン確認
    // ulongの右シフトは論理シフト（符号なし）なので最上位ビットのみ残り 1 になる
    const ulong top_bit = 0x8000000000000000;
    const ulong shifted = top_bit >> 63;
    println("top bit shift: {shifted}");

    // 定数演算
    const ulong a = 0xF000000000000000;
    const ulong b = 0x0000000000000001;
    println("sum check");

    println("PASS");
    return 0;
}
