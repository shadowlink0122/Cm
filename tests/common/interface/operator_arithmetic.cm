import std::io::println;

// 算術演算子オーバーロードテスト
struct Num {
    int value;
}

impl Num {
    operator Num +(Num other) {
        return Num{value: self.value + other.value};
    }

    operator Num -(Num other) {
        return Num{value: self.value - other.value};
    }

    operator Num *(Num other) {
        return Num{value: self.value * other.value};
    }

    operator Num /(Num other) {
        return Num{value: self.value / other.value};
    }

    operator Num %(Num other) {
        return Num{value: self.value % other.value};
    }
}

int main() {
    const Num a = Num{value: 10};
    const Num b = Num{value: 3};

    const Num sum = a + b;
    println("10 + 3 = {sum.value}");

    const Num diff = a - b;
    println("10 - 3 = {diff.value}");

    const Num prod = a * b;
    println("10 * 3 = {prod.value}");

    const Num quot = a / b;
    println("10 / 3 = {quot.value}");

    const Num rem = a % b;
    println("10 % 3 = {rem.value}");

    return 0;
}
