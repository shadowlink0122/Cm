// Move Semantics エラーテスト: 移動後の直接使用
// 期待: コンパイルエラー "Variable 's' used after move"
int main() {
    const int x = 42;
    const int y = move x;
    // 所有権をyへ移動
    const int z = x;
    // エラー: xは既にmove済み
    return z;
}
