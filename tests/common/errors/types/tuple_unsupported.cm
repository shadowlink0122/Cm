// タプル型・分解代入は未対応（専用診断で構造体使用へ誘導する）。
// (a, b) = 7, 1; のような分解代入の導入はタプル型/多値戻り値の設計が前提のため見送り（docs/archive/unimplemented/tuple-destructuring.md）
int main() {
    int a = 1;
    int b = 2;
    (a, b) = 7, 1;
    return 0;
}
