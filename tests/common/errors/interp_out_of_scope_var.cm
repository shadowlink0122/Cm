// 補間プレースホルダ内のスコープ外変数参照の検査:
// 旧: 型チェッカが補間内容を検査せず、ブロック外に出た変数の参照が
// コンパイルエラーにならずゴミ値（JSではnull）を出力していた
import std::io::println;

int main() {
    {
        int b = 42;
        println("in block: {b}");
    }
    // b はブロックスコープを抜けているため補間内でもエラーになるべき
    println("out block: {b}");
    return 0;
}
