// Test defer with continue
import std::io::println;

int main() {
    for (int i = 0; i < 4; i = i + 1) {
        defer println(100);
        if (i == 2) {
            continue;
        }
        println(i);
    }
    println(200);
    return 0;
}
