// Test defer with break
import std::io::println;

int main() {
    for (int i = 0; i < 5; i = i + 1) {
        defer println(100);
        println(i);
        if (i == 2) {
            break;
        }
    }
    println(200);
    return 0;
}
