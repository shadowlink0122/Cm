import std::io::println;
// match式のテスト - 文字列結果

int main() {
    const int status = 1;

    // 文字列を返すmatch式
    const string msg = match (status) {
        0 => "OK",
        1 => "ERROR",
        _ => "UNKNOWN",
    };

    println("status: {msg}");

    return 0;
}
