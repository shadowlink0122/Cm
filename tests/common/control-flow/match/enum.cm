import std::io::println;
// match式のテスト - 様々なパターン

enum Status {
    Ok = 0,
    Error = 1,
    Pending = 2
}

int main() {
    // 基本的なリテラルマッチ
    const int x = 3;
    const int result1 = match (x) {
        1 => 100,
        2 => 200,
        3 => 300,
        _ => 0,
    };
    println("match(3) = {result1}");

    // enum値マッチ（整数結果）
    const int status = Status::Error;
    const int code = match (status) {
        Status::Ok => 0,
        Status::Error => -1,
        Status::Pending => 1,
        _ => -99,
    };
    println("status code = {code}");

    // ワイルドカードパターン
    const int y = 99;
    const int result2 = match (y) {
        1 => 10,
        2 => 20,
        _ => 999,
    };
    println("match(99) = {result2}");

    return 0;
}
