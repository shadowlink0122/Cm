// R10: constジェネリックパラメータは値引数での実体化が未実装のため、宣言時に専用診断で拒否される（従来は宣言のみ無警告で受理され、使用箇所で無関係な構文エラーに化けていた）
import std::io::println;

struct Buffer<T, SIZE: const int> {
    T* data;
    int len;
}

int main() {
    println("should not compile");
    return 0;
}
