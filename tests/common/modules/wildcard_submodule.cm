import std::io::println;
// test_wildcard_submodule.cm - ワイルドカードサブモジュールインポート
import ./basic_reexport/std::io::*;

int main() {
    const int sum = add(10, 20);
    const int product = multiply(3, 4);
    println("Sum: {sum}");
    println("Product: {product}");
    return 0;
}
