// 境界外書き込み: ループ上限を実行時値にして定数畳み込みによる除去を防ぐ（-O0で実行する前提）
// 書き込み結果を出力で使用し、ストアがデッドコード削除されないようにする
int main() {
    int[4] arr;
    int n = 4;
    for (int i = 0; i <= n; i++) {
        arr[i] = i;
    }
    println("done {arr[0]}");
    return 0;
}
