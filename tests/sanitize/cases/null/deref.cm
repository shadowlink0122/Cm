// nullポインタ参照: --sanitize=undefined で panic になる（サニタイザ無しではSEGV等の未定義動作）
struct Node {
    int value;
}
int main() {
    Node* p = null as Node*;
    int v = p->value;
    println("v={v}");
    return 0;
}
