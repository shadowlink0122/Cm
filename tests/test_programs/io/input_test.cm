// 標準入力のテスト
import std::io;

int main() {
    println("=== Input Test ===");

    // 1. Line input
    const string name = input("Enter your name: ");
    println("Hello, " + name + "!");

    // 2. Int input
    const int age = input_int("Enter your age: ");
    println("Next year, you will be " + (age + 1) + ".");

    // 3. Multiple inputs
    print("Enter two numbers separated by space: ");
    const int a = input_int();
    const int b = input_int();
    println("Sum: " + (a + b));

    println("=== Test Completed ===");
    return 0;
}
