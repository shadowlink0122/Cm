// Phase 2: if文の基本テスト
import std::io::println;
int main() {
    int x = 10;

    // 条件が真
    if (x > 5) {
        println(1);
    }

    // 条件が偽
    if (x < 5) {
        println(2);  // 実行されない
    }

    // 条件が真（等価）
    if (x == 10) {
        println(3);
    }

    return 0;
}
