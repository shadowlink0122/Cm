import std::io::println;
// 基本的なmatch式のテスト

int main() {
    int x = 2;
    
    // 整数リテラルパターン
    int result = match (x) {
        1 => 10,
        2 => 20,
        3 => 30,
        _ => 0,
    };
    
    println("result = {result}");
    
    return 0;
}
