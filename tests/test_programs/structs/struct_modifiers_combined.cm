// private と default 修飾子の組み合わせテスト

struct Config {
    private string secret;      // 外部アクセス不可
    default int timeout;        // デフォルトメンバ
    string name;                // 通常メンバ
}

int main() {
    Config cfg;
    cfg.name = "app";
    cfg.timeout = 30;

    return cfg.timeout == 30 ? 0 : 1;
}
