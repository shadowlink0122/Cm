// 構造体を引数に取る関数
struct Point {
    int x;
    int y;
}

int sum_coords(Point p) {
    return p.x + p.y;
}

int main() {
    Point p;
    p.x = 10;
    p.y = 20;
    int result = sum_coords(p);
    println("{result}");
    return 0;
}
