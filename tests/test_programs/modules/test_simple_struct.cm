// test_simple_struct.cm - 構造体インポートのテスト

import simple_struct;

int main() {
    // 構造体の使用
    float x = 3.0;
    float y = 4.0;
    Point p = makePoint(x, y);
    printPoint(p);

    // 直接フィールドアクセス
    p.x = 5.0;
    p.y = 6.0;
    printPoint(p);

    return 0;
}