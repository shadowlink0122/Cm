// v4形式のmathモジュール
module math;

// トップレベルでconst宣言
const float PI = 3.14159265359;
const float E = 2.71828182846;
const float PHI = 1.61803398875;  // 黄金比

// 内部定数（エクスポートされない）
const float EPSILON = 0.000001;

// 関数定義
float sin(float angle) {
    // 簡略化された実装
    return angle;  // TODO: 実際の実装
}

float cos(float angle) {
    // 簡略化された実装
    return 1.0 - angle;  // TODO: 実際の実装
}

float abs(float x) {
    return x < 0 ? -x : x;
}

// 内部ヘルパー関数（エクスポートされない）
float clamp_internal(float value, float min, float max) {
    if (value < min) return min;
    if (value > max) return max;
    return value;
}

// v4形式のエクスポート（名前のみを列挙）
export PI, E, PHI;
export sin, cos, abs;
// EPSILON と clamp_internal はエクスポートされない