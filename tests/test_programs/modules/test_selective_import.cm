// v4モジュールシステムのテスト：選択的インポート

// Rust風の選択的インポート
import math::{PI, E, sin, cos};
import graphics::Point;
import graphics::{Rectangle as Rect, Circle};
import std::io::{println};

int main() {
    // インポートした定数を使用
    float angle = PI / 4.0;
    float result = sin(angle) + cos(angle);

    // インポートした型を使用
    Point p = {10.0, 20.0};
    Rect r = {{0.0, 0.0}, {100.0, 100.0}};
    Circle c = {p, 5.0};

    println("Result: {result}");

    return 0;
}