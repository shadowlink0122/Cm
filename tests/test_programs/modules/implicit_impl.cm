import std::io::println;
// test_implicit_impl.cm - implの暗黙的エクスポートとメソッド呼び出しテスト
import ./implicit_impl/shapes;

int main() {
    shapes::Point p;
    p.x = 10;
    p.y = 20;
    p.print();
    // 暗黙的にエクスポートされたimplのメソッド

    int val = shapes::get_value();
    println("Value: {val}");
    return 0;
}
