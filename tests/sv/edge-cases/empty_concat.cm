//! platform: sv
//! description: Empty concatenation edge case

#[input] bool clk = false;
#[output] uint result = 0;

async void empty_concat_test(posedge clk) {
    // 空の連接はSVでは特殊なケース
    // 直接的な空連接は型推論の問題があるため、代わりに単純なテストを行う
    result = 0;
}
