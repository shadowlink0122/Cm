//! platform: sv

// always_latch テスト: always_latch キーワードで明示的にラッチ指定

#[input]  bool enable = false;
#[input]  uint data_in = 0;
#[output] uint data_out = 0;

always_latch void latch_process() {
    if (enable) {
        data_out = data_in;
    }
}
