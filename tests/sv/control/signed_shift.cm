//! platform: sv
//! test: s=-8 -> shr2=-2
//! test: s=8 -> shr2=2

// 符号付き右シフトの回帰テスト:
// Cmの >> は符号付き型では算術シフト（LLVMバックエンドと同一意味論）。
// SVの >> は常に論理シフトのため、>>> が出力されないと
// 負数のシフト結果が巨大な正の値になる。

#[input] int s = 0;
#[output] int shr2 = 0;

void shift() {
    shr2 = s >> 2;
}
