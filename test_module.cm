// テスト用モジュールファイル
module test.example;

import std.io.{println, print};
import std.core.Option;

export struct Point {
    int x;
    int y;
};

export int calculate(int a, int b) {
    return a + b;
}

int main() {
    println("Testing module system");
    Point p = { .x = 10, .y = 20 };
    int result = calculate(p.x, p.y);
    return 0;
}